<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ButtonNavigation">

    <item
        android:id="@+id/action_today"
        android:icon="@drawable/ic_event"
        android:orderInCategory="100"
        android:title="Hoy"
        app:showAsAction="ifRoom" />

    <group android:checkableBehavior="single">

        <item
            android:id="@+id/action_day_view"
            android:orderInCategory="100"
            android:title="Diaria"
            app:showAsAction="never" />

        <item
            android:id="@+id/action_three_day_view"
            android:checked="true"
            android:orderInCategory="200"
            android:title="3 Dias"
            app:showAsAction="never" />

        <item
            android:id="@+id/action_week_view"
            android:orderInCategory="300"
            android:title="Semanal"
            app:showAsAction="never" />

    </group>

</menu>